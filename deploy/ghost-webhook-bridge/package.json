{
  "name": "ghost-webhook-bridge",
  "version": "1.0.0",
  "description": "Receives Ghost post.published webhooks and creates Listmonk campaigns",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
